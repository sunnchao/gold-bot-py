services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: gold-bot-backend:local
    pull_policy: build

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: gold-bot-frontend:local
    pull_policy: build
